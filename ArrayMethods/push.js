const numbers = [1, 2, 3, 4, 5, 6];
console.clear();

// Add element at the end of the array
console.log(numbers.push(11, 12)); // length of an array 8
console.log(numbers); //[1, 2,  3,  4, 5, 6, 11, 12]

// Remove element at the end of the array
// console.log(numbers.pop()); // removed element 6
// console.log(numbers); //[ 1, 2, 3, 4, 5 ]

// Add element at the starting of the array
// console.log(numbers.unshift(11, 12)); // length of an array 8
// console.log(numbers); //[11, 12, 1, 2,  3,  4, 5, 6]

// Remove element at the starting of the array
// console.log(numbers.shift()); // removed element 1
// console.log(numbers); //[2, 3, 4, 5, 6 ]
